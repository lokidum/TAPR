services:
  postgres-test:
    image: postgis/postgis:15-3.3
    environment:
      POSTGRES_USER: tapr_test
      POSTGRES_PASSWORD: testpassword
      POSTGRES_DB: tapr_test
    ports:
      - "5433:5432"
